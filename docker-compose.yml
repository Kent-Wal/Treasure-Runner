services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: treasure-runner-dev:latest
    container_name: treasure-runner-dev
    working_dir: /workspace
    volumes:
      - .:/workspace
    stdin_open: true
    tty: true
    command: bash
